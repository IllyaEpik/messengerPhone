import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tab:{
    flex: 1,
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    // borderWidth: 0,
    borderTopColor: '#070A1C',
    paddingVertical: 10,
    paddingHorizontal: 35
  },
  selected:{
    borderTopWidth:3
  }

})