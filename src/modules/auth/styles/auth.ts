import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  title: {
    fontSize: 18,
    fontWeight: "700",
    marginBottom: 4,
    color: "#000",
  },

  subtitle: {
    fontSize: 20,
    textAlign: "center",
    marginBottom: 16,
    color: "#000"
  
  },
  button: {
    width: "100%",
  }
});